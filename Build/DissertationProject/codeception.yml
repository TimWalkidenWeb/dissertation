paths:
    tests: tests
    output: tests/_output
    data: tests/_data
    support: tests/_support
    envs: tests/_envs
actor_suffix: Tester
extensions:
    enabled:
        - Codeception\Extension\RunFailed
modules:
        config:
        DB:
            dsn: 'mysql:host=localhost;dbname=dissertationadvert'
            user: 'root'
            password: 'webdev'